# UI Update Instructions for Test API Page

Replace the UI section (from line 323 onwards) with the following:

1. Add a "Reload Data" button in the header
2. Replace all Input fields with Select dropdowns for:
   - Rooms (selectedRoomCode)
   - Services (selectedServiceCode)
   - Employees (selectedEmployeeCode)
   - Patients (selectedPatientCode)
3. Add multi-select checkboxes for:
   - Service Codes (selectedServiceCodes)
   - Participant Codes (selectedParticipantCodes)
4. Keep Input for:
   - Date (availableDate)
   - Appointment Start Time (appointmentStartTime) - auto-filled from available times
   - Notes (appointmentNotes)

This will be done in the next update to avoid file length issues.


